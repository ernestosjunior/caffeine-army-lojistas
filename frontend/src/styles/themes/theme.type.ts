export interface Theme {
  colors: {
    white: string;
    primary: string;
    gray: string;
    lightGray: string;
    mediumGray: string;
    background: string;
    purple: string;
  };
  fontWeights: {
    extraLight: string;
    light: string;
    regular: string;
    semibold: string;
    bold: string;
  };
  fontFamily: {
    primary: string;
  };
}
